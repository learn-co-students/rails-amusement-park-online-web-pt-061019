<%= @attraction.name %>
<%= if current_user.admin == true %>
    <%= link_to "Edit Attraction", edit_attraction_path(@attraction) %>
    <%= elsif current)user.adim == false %>
    <%= render partial: "rides/form", locals: {ride: @ride, user: current_user, attraction: @attraction} %>
    <% end %>