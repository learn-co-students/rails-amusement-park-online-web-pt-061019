<%= form_tag("/signin", method: :post) do %>
    Your Name: <%= collection_select(:user, :name, User.all, :name, :name)%>
    <br>
    Your password: <%= password_field_tag :password %>
    <br>
    <%= submit_tag "Sign In" %>
<% end %>